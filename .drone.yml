workspace:
  base: /drone/src
  path: /${DRONE_REPO}/${DRONE_BRANCH}
clone:
  git:
    image: plugins/git
  git2:
    image: plugins/git
    commands:
      - git diff ${DRONE_PREV_COMMIT_SHA}...${DRONE_COMMIT_SHA} --name-statu
pipeline:
  test-prep:
    image: mongo
    commands:
    #- mongoimport --db test_db --collection widgets --type json --file seed.json --jsonArray
    - ls
  test:
    image: mongo
    commands:
    #- mongo --eval "db.widgets.find({});" test_db
    - pwd

services:
  mongo:
    image: mongo
